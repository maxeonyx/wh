<Window x:Class="wh.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="wh — Whisper HUD" Height="220" Width="420"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource ForegroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Whisper on Windows HUD (M1)" FontSize="18" FontWeight="SemiBold"/>
        <Button x:Name="RecordButton" Grid.Row="1" Height="42" Content="Record" Margin="0,12,0,0"
                Background="{StaticResource AccentBrush}"
                Foreground="White" BorderBrush="Transparent"
                Click="OnRecordClick" />
        <TextBlock x:Name="NativeHello" Grid.Row="2" TextWrapping="Wrap" Margin="0,12,0,0"/>
    </Grid>
    
</Window>
