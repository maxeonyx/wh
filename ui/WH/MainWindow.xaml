<Window x:Class="wh.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="wh - Whisper HUD" Height="220" Width="420"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource ForegroundBrush}"
        WindowStartupLocation="CenterScreen"
        ShowActivated="False"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Whisper on Windows HUD" FontSize="18" FontWeight="SemiBold"/>
        <Border Grid.Row="1" Height="42" Margin="0,12,0,0" Background="#1E1E1E" CornerRadius="6" Padding="12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="12" Height="12" Fill="{StaticResource AccentBrush}"/>
                <TextBlock x:Name="StatusText" Text=" Transcribing..." Margin="8,0,0,0"/>
            </StackPanel>
        </Border>
        <TextBlock x:Name="TranscriptText"
                   Grid.Row="2"
                   TextWrapping="Wrap"
                   Margin="0,12,0,0"
                   AutomationProperties.AutomationId="TranscriptText"/>
    </Grid>
    
</Window>
